#:include design/screen/share/special_label.kv

<CallInfoWidget>:
	height: dp(100) # WORKAROUND, content doesn't show well unless height is fixed
	size_hint_y: None

	BoxLayout:
		orientation: 'vertical'
		BoxLayout:
			ZETLabel:
				id: nameId
				text: root.driverName
				font_size: '15dp'
				
			IconRightWidget:
				#pos_hint: {'center_y': .5}
				halign: 'left'
				icon: 'content-copy'
				on_release: root.copyNameOnClipboard()
		BoxLayout:
			ZETLabel:
				id: phoneNumberId
				text: root.driverPhoneNumber
				font_size: '15dp'
				
			MDIconButton:
				pos_hint: {'center_y': .5}
				icon: 'content-copy'
				on_release: root.copyPhoneNumberOnClipboard()
